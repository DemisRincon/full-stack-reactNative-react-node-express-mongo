import {StyleSheet} from 'react-native';
import {globalConfiguration} from '../../library/styles/global';

const styles = StyleSheet.create({
  container: {
    backgroundColor: globalConfiguration.colors.background,
  },
});
export default styles;
